# Models

This folder is intended to store trained neural network weights (.pth files).

Model checkpoints are not included in this repository because they can be regenerated by running the training notebooks.